apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: claude-code-workspace
  namespace: claude-code
spec:
  storageClassName: ssd-nfs-client
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 256Gi
