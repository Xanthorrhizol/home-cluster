apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TCPRoute
metadata:
  name: postgres
  namespace: database
spec:
  parentRefs:
    - name: home-gateway
      namespace: envoy-gateway-system
      sectionName: postgres
  rules:
    - backendRefs:
        - name: db-rw
          port: 5432
